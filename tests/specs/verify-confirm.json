[
  {
    "name": "verify:confirm button verifies the user",
    "steps": [
      {
        "send": { "callback": "verify:confirm" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": {
              "text": "✅ You're verified! Welcome to the group."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "verify:confirm shows already-verified if already verified",
    "steps": [
      {
        "send": { "callback": "verify:confirm" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": {
              "text": "✅ You're verified! Welcome to the group."
            }
          }
        ]
      },
      {
        "send": { "callback": "verify:confirm" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": {
              "text": "✅ You're already verified."
            }
          }
        ]
      }
    ]
  }
]
